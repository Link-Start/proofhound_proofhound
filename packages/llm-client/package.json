{
  "name": "@proofhound/llm-client",
  "version": "0.0.0",
  "private": true,
  "description": "LLM 调用包装 — 含 docs/specs/05-logging.md §5.6 日志契约 + 限流前置",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "test:unit": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@proofhound/db": "workspace:*",
    "@proofhound/limiter": "workspace:*",
    "@proofhound/logger": "workspace:*",
    "@proofhound/shared": "workspace:*",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
